<p>If you would like to transfer out your captain please select a new captain from below.</p>
<p>Remember captains cost one transfer, score you double points and can't be one of your favourites.</p>
<div class="form-group">
  <label for="team_captain_id">Captain</label>
  <select class="form-control" name="team[captain_id]" id="team_captain_id" <%= 'disabled' unless @current_team_captain.available_for_transfer? %>>
    <% if @captain_options.present? %>
    <%= @current_team_captain %>
      <% @captain_options.each do |option| %>
        <% next unless option == @current_team_captain || option.available_for_transfer? %>
        <option
          value="<%= option.id %>"<%= @current_captain_selection == option ? 'selected' : ''%>
        >
          <%= option.name %> for <%= option.competition.name %>
        </option>
      <% end %>
    <% else %>
        <option disabled selected value> -- Captain options will appear here -- </option>
    <% end %>
  </select>
</div>
